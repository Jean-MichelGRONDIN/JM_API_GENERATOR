import { Router } from "express";
$ROUTER_MIDDLEWARE_IMPORTS$
$ROUTER_SANITIZER_IMPORTS$
$ROUTER_REQUEST_IMPORTS$
$ROUTER_CONTROLLER_IMPORTS$

const router = Router();

$ROUTER_ROUTES$

export default router;
